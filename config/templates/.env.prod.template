# ===========================================
# PRODUCTION ENVIRONMENT
# ===========================================
#
# This file is for casovispanskog.rs production deployment.
# Deploy to: ~/spanish-class-backend/.env
#
# INSTRUCTIONS:
# 1. Copy this file: cp config/templates/.env.prod.template config/prod/.env
# 2. Update ALL placeholder values with STRONG production secrets
# 3. Deploy: ./scripts/deploy/deploy-prod.sh --with-config
#
# SECURITY WARNING:
# - Use STRONG, UNIQUE passwords and secrets
# - NEVER use development credentials in production
# - NEVER commit this file to git
#

# Environment
NODE_ENV=production
PORT=3001

# Database - Production Database on cPanel
# Format: mysql://[CPANEL_USER]_[DB_USER]:[PASSWORD]@localhost:3306/[CPANEL_USER]_[DB_NAME]
# IMPORTANT: Update with actual production credentials
DATABASE_URL="mysql://gigovicr_thekgiga:Kragujevac.94@localhost:3306/gigovicr_spanish_class"

# JWT Authentication
# CRITICAL: Generate STRONG unique secret with: openssl rand -base64 32
# This MUST be different from dev/local secrets!
JWT_SECRET="CHANGE_ME_PRODUCTION_SECRET_MUST_BE_STRONG_MIN_32_CHARS"
JWT_EXPIRES_IN="7d"

# Application URLs
FRONTEND_URL="https://casovispanskog.rs"
API_URL="https://casovispanskog.rs/api"

# Email Service (REQUIRED for production)
# Get API key from: https://resend.com
RESEND_API_KEY="re_CHANGE_ME_production_api_key"
EMAIL_FROM="Spanish Class <noreply@casovispanskog.rs>"
PROFESSOR_EMAIL="professor@casovispanskog.rs"

# Google Calendar API (Required if using calendar features)
# GOOGLE_SERVICE_ACCOUNT_EMAIL="your-service@project.iam.gserviceaccount.com"
# GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour_Key\n-----END PRIVATE KEY-----"
# GOOGLE_CALENDAR_ID="primary"

# Jitsi Video Conferencing (Optional)
# JITSI_APP_ID=""
# JITSI_API_KEY=""

# Logging
LOG_LEVEL="error"
