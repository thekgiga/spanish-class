# Redesign Evidence

Create one evidence file per coherent frontend slice. Name it:

`YYYY-MM-DD-requirement-or-flow.md`

Store screenshot paths or CI artifact links rather than binary screenshots in git when repository policy prefers external artifacts.
