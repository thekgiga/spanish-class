# ADR-001 — One-to-one is the primary experience

**Status:** Accepted

The primary UI is designed for one professor and one student per lesson. Legacy group/waitlist capabilities may remain in the backend during migration but must not shape the default information architecture or interaction model without a new decision.
